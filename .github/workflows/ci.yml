name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: bufbuild/buf-setup-action@v1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: docker run --rm -v "$PWD:/workspace" -w /workspace bufbuild/buf:latest lint
      - name: Check breaking changes
        if: github.event_name == 'pull_request'
        run: |
          if git rev-parse --verify origin/main >/dev/null 2>&1; then
            docker run --rm -v "$PWD:/workspace" -w /workspace bufbuild/buf:latest breaking --against '.git#branch=main'
          else
            echo "origin/main not found; skipping breaking check"
          fi
      - run: docker run --rm -v "$PWD:/workspace" -w /workspace bufbuild/buf:latest generate
      - run: npm run build
      - run: go mod tidy
      - run: git diff --exit-code
