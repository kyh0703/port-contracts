// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: port/media/v1/media.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "port.media.v1";

export interface CreateSessionRequest {
  sessionId: string;
  conversationId: string;
}

export interface CreateSessionResponse {
  sessionId: string;
  conversationId: string;
  roomId: string;
  status: string;
}

export interface GetSessionStatusRequest {
  sessionId: string;
}

export interface GetSessionStatusResponse {
  sessionId: string;
  conversationId: string;
  userId: string;
  roomId: string;
  status: string;
  connectionState: string;
  mediaState: string;
  participants: number;
  participantStates: ParticipantState[];
  lastRuntimeEventType: string;
  lastRuntimeEventAt?: Date | undefined;
  recentRuntimeEvents: RuntimeEvent[];
  startedAt?: Date | undefined;
  lastActiveAt?: Date | undefined;
}

export interface ParticipantState {
  id: string;
  role: string;
  audioMode: string;
  connectionState: string;
  tracks: number;
}

export interface RuntimeEvent {
  type: string;
  at?: Date | undefined;
}

export interface LeaveParticipantRequest {
  sessionId: string;
  participantId: string;
}

export interface LeaveParticipantResponse {
  sessionId: string;
  roomId: string;
  participantId: string;
  status: string;
}

export interface EndSessionRequest {
  sessionId: string;
}

export interface EndSessionResponse {
  sessionId: string;
  roomId: string;
  status: string;
}

function createBaseCreateSessionRequest(): CreateSessionRequest {
  return { sessionId: "", conversationId: "" };
}

export const CreateSessionRequest: MessageFns<CreateSessionRequest> = {
  encode(message: CreateSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.conversationId !== "") {
      writer.uint32(18).string(message.conversationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
    };
  },

  toJSON(message: CreateSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    return CreateSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    const message = createBaseCreateSessionRequest();
    message.sessionId = object.sessionId ?? "";
    message.conversationId = object.conversationId ?? "";
    return message;
  },
};

function createBaseCreateSessionResponse(): CreateSessionResponse {
  return { sessionId: "", conversationId: "", roomId: "", status: "" };
}

export const CreateSessionResponse: MessageFns<CreateSessionResponse> = {
  encode(message: CreateSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.conversationId !== "") {
      writer.uint32(18).string(message.conversationId);
    }
    if (message.roomId !== "") {
      writer.uint32(26).string(message.roomId);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: CreateSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    return CreateSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    const message = createBaseCreateSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.conversationId = object.conversationId ?? "";
    message.roomId = object.roomId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseGetSessionStatusRequest(): GetSessionStatusRequest {
  return { sessionId: "" };
}

export const GetSessionStatusRequest: MessageFns<GetSessionStatusRequest> = {
  encode(message: GetSessionStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSessionStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSessionStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSessionStatusRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: GetSessionStatusRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetSessionStatusRequest>): GetSessionStatusRequest {
    return GetSessionStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSessionStatusRequest>): GetSessionStatusRequest {
    const message = createBaseGetSessionStatusRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseGetSessionStatusResponse(): GetSessionStatusResponse {
  return {
    sessionId: "",
    conversationId: "",
    userId: "",
    roomId: "",
    status: "",
    connectionState: "",
    mediaState: "",
    participants: 0,
    participantStates: [],
    lastRuntimeEventType: "",
    lastRuntimeEventAt: undefined,
    recentRuntimeEvents: [],
    startedAt: undefined,
    lastActiveAt: undefined,
  };
}

export const GetSessionStatusResponse: MessageFns<GetSessionStatusResponse> = {
  encode(message: GetSessionStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.conversationId !== "") {
      writer.uint32(18).string(message.conversationId);
    }
    if (message.userId !== "") {
      writer.uint32(26).string(message.userId);
    }
    if (message.roomId !== "") {
      writer.uint32(34).string(message.roomId);
    }
    if (message.status !== "") {
      writer.uint32(42).string(message.status);
    }
    if (message.connectionState !== "") {
      writer.uint32(50).string(message.connectionState);
    }
    if (message.mediaState !== "") {
      writer.uint32(58).string(message.mediaState);
    }
    if (message.participants !== 0) {
      writer.uint32(64).int32(message.participants);
    }
    for (const v of message.participantStates) {
      ParticipantState.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.lastRuntimeEventType !== "") {
      writer.uint32(82).string(message.lastRuntimeEventType);
    }
    if (message.lastRuntimeEventAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastRuntimeEventAt), writer.uint32(90).fork()).join();
    }
    for (const v of message.recentRuntimeEvents) {
      RuntimeEvent.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(106).fork()).join();
    }
    if (message.lastActiveAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastActiveAt), writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSessionStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSessionStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.connectionState = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mediaState = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.participants = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.participantStates.push(ParticipantState.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastRuntimeEventType = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.lastRuntimeEventAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.recentRuntimeEvents.push(RuntimeEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.lastActiveAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSessionStatusResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      connectionState: isSet(object.connectionState)
        ? globalThis.String(object.connectionState)
        : isSet(object.connection_state)
        ? globalThis.String(object.connection_state)
        : "",
      mediaState: isSet(object.mediaState)
        ? globalThis.String(object.mediaState)
        : isSet(object.media_state)
        ? globalThis.String(object.media_state)
        : "",
      participants: isSet(object.participants) ? globalThis.Number(object.participants) : 0,
      participantStates: globalThis.Array.isArray(object?.participantStates)
        ? object.participantStates.map((e: any) => ParticipantState.fromJSON(e))
        : globalThis.Array.isArray(object?.participant_states)
        ? object.participant_states.map((e: any) => ParticipantState.fromJSON(e))
        : [],
      lastRuntimeEventType: isSet(object.lastRuntimeEventType)
        ? globalThis.String(object.lastRuntimeEventType)
        : isSet(object.last_runtime_event_type)
        ? globalThis.String(object.last_runtime_event_type)
        : "",
      lastRuntimeEventAt: isSet(object.lastRuntimeEventAt)
        ? fromJsonTimestamp(object.lastRuntimeEventAt)
        : isSet(object.last_runtime_event_at)
        ? fromJsonTimestamp(object.last_runtime_event_at)
        : undefined,
      recentRuntimeEvents: globalThis.Array.isArray(object?.recentRuntimeEvents)
        ? object.recentRuntimeEvents.map((e: any) => RuntimeEvent.fromJSON(e))
        : globalThis.Array.isArray(object?.recent_runtime_events)
        ? object.recent_runtime_events.map((e: any) => RuntimeEvent.fromJSON(e))
        : [],
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      lastActiveAt: isSet(object.lastActiveAt)
        ? fromJsonTimestamp(object.lastActiveAt)
        : isSet(object.last_active_at)
        ? fromJsonTimestamp(object.last_active_at)
        : undefined,
    };
  },

  toJSON(message: GetSessionStatusResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.connectionState !== "") {
      obj.connectionState = message.connectionState;
    }
    if (message.mediaState !== "") {
      obj.mediaState = message.mediaState;
    }
    if (message.participants !== 0) {
      obj.participants = Math.round(message.participants);
    }
    if (message.participantStates?.length) {
      obj.participantStates = message.participantStates.map((e) => ParticipantState.toJSON(e));
    }
    if (message.lastRuntimeEventType !== "") {
      obj.lastRuntimeEventType = message.lastRuntimeEventType;
    }
    if (message.lastRuntimeEventAt !== undefined) {
      obj.lastRuntimeEventAt = message.lastRuntimeEventAt.toISOString();
    }
    if (message.recentRuntimeEvents?.length) {
      obj.recentRuntimeEvents = message.recentRuntimeEvents.map((e) => RuntimeEvent.toJSON(e));
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.lastActiveAt !== undefined) {
      obj.lastActiveAt = message.lastActiveAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<GetSessionStatusResponse>): GetSessionStatusResponse {
    return GetSessionStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSessionStatusResponse>): GetSessionStatusResponse {
    const message = createBaseGetSessionStatusResponse();
    message.sessionId = object.sessionId ?? "";
    message.conversationId = object.conversationId ?? "";
    message.userId = object.userId ?? "";
    message.roomId = object.roomId ?? "";
    message.status = object.status ?? "";
    message.connectionState = object.connectionState ?? "";
    message.mediaState = object.mediaState ?? "";
    message.participants = object.participants ?? 0;
    message.participantStates = object.participantStates?.map((e) => ParticipantState.fromPartial(e)) || [];
    message.lastRuntimeEventType = object.lastRuntimeEventType ?? "";
    message.lastRuntimeEventAt = object.lastRuntimeEventAt ?? undefined;
    message.recentRuntimeEvents = object.recentRuntimeEvents?.map((e) => RuntimeEvent.fromPartial(e)) || [];
    message.startedAt = object.startedAt ?? undefined;
    message.lastActiveAt = object.lastActiveAt ?? undefined;
    return message;
  },
};

function createBaseParticipantState(): ParticipantState {
  return { id: "", role: "", audioMode: "", connectionState: "", tracks: 0 };
}

export const ParticipantState: MessageFns<ParticipantState> = {
  encode(message: ParticipantState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.role !== "") {
      writer.uint32(18).string(message.role);
    }
    if (message.audioMode !== "") {
      writer.uint32(26).string(message.audioMode);
    }
    if (message.connectionState !== "") {
      writer.uint32(34).string(message.connectionState);
    }
    if (message.tracks !== 0) {
      writer.uint32(40).int32(message.tracks);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParticipantState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.role = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.audioMode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connectionState = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tracks = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantState {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      audioMode: isSet(object.audioMode)
        ? globalThis.String(object.audioMode)
        : isSet(object.audio_mode)
        ? globalThis.String(object.audio_mode)
        : "",
      connectionState: isSet(object.connectionState)
        ? globalThis.String(object.connectionState)
        : isSet(object.connection_state)
        ? globalThis.String(object.connection_state)
        : "",
      tracks: isSet(object.tracks) ? globalThis.Number(object.tracks) : 0,
    };
  },

  toJSON(message: ParticipantState): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.audioMode !== "") {
      obj.audioMode = message.audioMode;
    }
    if (message.connectionState !== "") {
      obj.connectionState = message.connectionState;
    }
    if (message.tracks !== 0) {
      obj.tracks = Math.round(message.tracks);
    }
    return obj;
  },

  create(base?: DeepPartial<ParticipantState>): ParticipantState {
    return ParticipantState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParticipantState>): ParticipantState {
    const message = createBaseParticipantState();
    message.id = object.id ?? "";
    message.role = object.role ?? "";
    message.audioMode = object.audioMode ?? "";
    message.connectionState = object.connectionState ?? "";
    message.tracks = object.tracks ?? 0;
    return message;
  },
};

function createBaseRuntimeEvent(): RuntimeEvent {
  return { type: "", at: undefined };
}

export const RuntimeEvent: MessageFns<RuntimeEvent> = {
  encode(message: RuntimeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.at !== undefined) {
      Timestamp.encode(toTimestamp(message.at), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuntimeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuntimeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuntimeEvent {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      at: isSet(object.at) ? fromJsonTimestamp(object.at) : undefined,
    };
  },

  toJSON(message: RuntimeEvent): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.at !== undefined) {
      obj.at = message.at.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<RuntimeEvent>): RuntimeEvent {
    return RuntimeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuntimeEvent>): RuntimeEvent {
    const message = createBaseRuntimeEvent();
    message.type = object.type ?? "";
    message.at = object.at ?? undefined;
    return message;
  },
};

function createBaseLeaveParticipantRequest(): LeaveParticipantRequest {
  return { sessionId: "", participantId: "" };
}

export const LeaveParticipantRequest: MessageFns<LeaveParticipantRequest> = {
  encode(message: LeaveParticipantRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.participantId !== "") {
      writer.uint32(18).string(message.participantId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeaveParticipantRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveParticipantRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.participantId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveParticipantRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      participantId: isSet(object.participantId)
        ? globalThis.String(object.participantId)
        : isSet(object.participant_id)
        ? globalThis.String(object.participant_id)
        : "",
    };
  },

  toJSON(message: LeaveParticipantRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.participantId !== "") {
      obj.participantId = message.participantId;
    }
    return obj;
  },

  create(base?: DeepPartial<LeaveParticipantRequest>): LeaveParticipantRequest {
    return LeaveParticipantRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LeaveParticipantRequest>): LeaveParticipantRequest {
    const message = createBaseLeaveParticipantRequest();
    message.sessionId = object.sessionId ?? "";
    message.participantId = object.participantId ?? "";
    return message;
  },
};

function createBaseLeaveParticipantResponse(): LeaveParticipantResponse {
  return { sessionId: "", roomId: "", participantId: "", status: "" };
}

export const LeaveParticipantResponse: MessageFns<LeaveParticipantResponse> = {
  encode(message: LeaveParticipantResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.participantId !== "") {
      writer.uint32(26).string(message.participantId);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeaveParticipantResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveParticipantResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.participantId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveParticipantResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      participantId: isSet(object.participantId)
        ? globalThis.String(object.participantId)
        : isSet(object.participant_id)
        ? globalThis.String(object.participant_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: LeaveParticipantResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.participantId !== "") {
      obj.participantId = message.participantId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<LeaveParticipantResponse>): LeaveParticipantResponse {
    return LeaveParticipantResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LeaveParticipantResponse>): LeaveParticipantResponse {
    const message = createBaseLeaveParticipantResponse();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.participantId = object.participantId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseEndSessionRequest(): EndSessionRequest {
  return { sessionId: "" };
}

export const EndSessionRequest: MessageFns<EndSessionRequest> = {
  encode(message: EndSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EndSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: EndSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<EndSessionRequest>): EndSessionRequest {
    return EndSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EndSessionRequest>): EndSessionRequest {
    const message = createBaseEndSessionRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseEndSessionResponse(): EndSessionResponse {
  return { sessionId: "", roomId: "", status: "" };
}

export const EndSessionResponse: MessageFns<EndSessionResponse> = {
  encode(message: EndSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EndSessionResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: EndSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<EndSessionResponse>): EndSessionResponse {
    return EndSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EndSessionResponse>): EndSessionResponse {
    const message = createBaseEndSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

export type MediaServiceService = typeof MediaServiceService;
export const MediaServiceService = {
  createSession: {
    path: "/port.media.v1.MediaService/CreateSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateSessionRequest): Buffer => Buffer.from(CreateSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateSessionRequest => CreateSessionRequest.decode(value),
    responseSerialize: (value: CreateSessionResponse): Buffer =>
      Buffer.from(CreateSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateSessionResponse => CreateSessionResponse.decode(value),
  },
  getSessionStatus: {
    path: "/port.media.v1.MediaService/GetSessionStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSessionStatusRequest): Buffer =>
      Buffer.from(GetSessionStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSessionStatusRequest => GetSessionStatusRequest.decode(value),
    responseSerialize: (value: GetSessionStatusResponse): Buffer =>
      Buffer.from(GetSessionStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSessionStatusResponse => GetSessionStatusResponse.decode(value),
  },
  leaveParticipant: {
    path: "/port.media.v1.MediaService/LeaveParticipant" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: LeaveParticipantRequest): Buffer =>
      Buffer.from(LeaveParticipantRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LeaveParticipantRequest => LeaveParticipantRequest.decode(value),
    responseSerialize: (value: LeaveParticipantResponse): Buffer =>
      Buffer.from(LeaveParticipantResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LeaveParticipantResponse => LeaveParticipantResponse.decode(value),
  },
  endSession: {
    path: "/port.media.v1.MediaService/EndSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: EndSessionRequest): Buffer => Buffer.from(EndSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): EndSessionRequest => EndSessionRequest.decode(value),
    responseSerialize: (value: EndSessionResponse): Buffer => Buffer.from(EndSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): EndSessionResponse => EndSessionResponse.decode(value),
  },
} as const;

export interface MediaServiceServer extends UntypedServiceImplementation {
  createSession: handleUnaryCall<CreateSessionRequest, CreateSessionResponse>;
  getSessionStatus: handleUnaryCall<GetSessionStatusRequest, GetSessionStatusResponse>;
  leaveParticipant: handleUnaryCall<LeaveParticipantRequest, LeaveParticipantResponse>;
  endSession: handleUnaryCall<EndSessionRequest, EndSessionResponse>;
}

export interface MediaServiceClient extends Client {
  createSession(
    request: CreateSessionRequest,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  createSession(
    request: CreateSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  createSession(
    request: CreateSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  getSessionStatus(
    request: GetSessionStatusRequest,
    callback: (error: ServiceError | null, response: GetSessionStatusResponse) => void,
  ): ClientUnaryCall;
  getSessionStatus(
    request: GetSessionStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSessionStatusResponse) => void,
  ): ClientUnaryCall;
  getSessionStatus(
    request: GetSessionStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSessionStatusResponse) => void,
  ): ClientUnaryCall;
  leaveParticipant(
    request: LeaveParticipantRequest,
    callback: (error: ServiceError | null, response: LeaveParticipantResponse) => void,
  ): ClientUnaryCall;
  leaveParticipant(
    request: LeaveParticipantRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LeaveParticipantResponse) => void,
  ): ClientUnaryCall;
  leaveParticipant(
    request: LeaveParticipantRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LeaveParticipantResponse) => void,
  ): ClientUnaryCall;
  endSession(
    request: EndSessionRequest,
    callback: (error: ServiceError | null, response: EndSessionResponse) => void,
  ): ClientUnaryCall;
  endSession(
    request: EndSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: EndSessionResponse) => void,
  ): ClientUnaryCall;
  endSession(
    request: EndSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: EndSessionResponse) => void,
  ): ClientUnaryCall;
}

export const MediaServiceClient = makeGenericClientConstructor(
  MediaServiceService,
  "port.media.v1.MediaService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): MediaServiceClient;
  service: typeof MediaServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
