// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: port/gateway/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "port.gateway.v1";

export interface DispatchConversationRequest {
  conversationId: string;
  sessionId: string;
  roomId: string;
  mediaSignalingUrl: string;
  serviceTokens?: ServiceTokens | undefined;
  capabilities?: Capabilities | undefined;
  recording?: RecordingConfig | undefined;
  mediaServerId: string;
}

export interface ServiceTokens {
  agent?: ParticipantToken | undefined;
  recorder?: ParticipantToken | undefined;
}

export interface ParticipantToken {
  participantId: string;
  token: string;
}

export interface Capabilities {
  attachAgent: boolean;
  startRecording: boolean;
}

export interface RecordingConfig {
  enabled: boolean;
}

export interface DispatchConversationResponse {
  conversationId: string;
  sessionId: string;
  roomId: string;
  status: string;
}

function createBaseDispatchConversationRequest(): DispatchConversationRequest {
  return {
    conversationId: "",
    sessionId: "",
    roomId: "",
    mediaSignalingUrl: "",
    serviceTokens: undefined,
    capabilities: undefined,
    recording: undefined,
    mediaServerId: "",
  };
}

export const DispatchConversationRequest: MessageFns<DispatchConversationRequest> = {
  encode(message: DispatchConversationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conversationId !== "") {
      writer.uint32(10).string(message.conversationId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(26).string(message.roomId);
    }
    if (message.mediaSignalingUrl !== "") {
      writer.uint32(34).string(message.mediaSignalingUrl);
    }
    if (message.serviceTokens !== undefined) {
      ServiceTokens.encode(message.serviceTokens, writer.uint32(42).fork()).join();
    }
    if (message.capabilities !== undefined) {
      Capabilities.encode(message.capabilities, writer.uint32(50).fork()).join();
    }
    if (message.recording !== undefined) {
      RecordingConfig.encode(message.recording, writer.uint32(58).fork()).join();
    }
    if (message.mediaServerId !== "") {
      writer.uint32(66).string(message.mediaServerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchConversationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDispatchConversationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mediaSignalingUrl = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceTokens = ServiceTokens.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.capabilities = Capabilities.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.recording = RecordingConfig.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.mediaServerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DispatchConversationRequest {
    return {
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      mediaSignalingUrl: isSet(object.mediaSignalingUrl)
        ? globalThis.String(object.mediaSignalingUrl)
        : isSet(object.media_signaling_url)
        ? globalThis.String(object.media_signaling_url)
        : "",
      serviceTokens: isSet(object.serviceTokens)
        ? ServiceTokens.fromJSON(object.serviceTokens)
        : isSet(object.service_tokens)
        ? ServiceTokens.fromJSON(object.service_tokens)
        : undefined,
      capabilities: isSet(object.capabilities) ? Capabilities.fromJSON(object.capabilities) : undefined,
      recording: isSet(object.recording) ? RecordingConfig.fromJSON(object.recording) : undefined,
      mediaServerId: isSet(object.mediaServerId)
        ? globalThis.String(object.mediaServerId)
        : isSet(object.media_server_id)
        ? globalThis.String(object.media_server_id)
        : "",
    };
  },

  toJSON(message: DispatchConversationRequest): unknown {
    const obj: any = {};
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.mediaSignalingUrl !== "") {
      obj.mediaSignalingUrl = message.mediaSignalingUrl;
    }
    if (message.serviceTokens !== undefined) {
      obj.serviceTokens = ServiceTokens.toJSON(message.serviceTokens);
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = Capabilities.toJSON(message.capabilities);
    }
    if (message.recording !== undefined) {
      obj.recording = RecordingConfig.toJSON(message.recording);
    }
    if (message.mediaServerId !== "") {
      obj.mediaServerId = message.mediaServerId;
    }
    return obj;
  },

  create(base?: DeepPartial<DispatchConversationRequest>): DispatchConversationRequest {
    return DispatchConversationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DispatchConversationRequest>): DispatchConversationRequest {
    const message = createBaseDispatchConversationRequest();
    message.conversationId = object.conversationId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.mediaSignalingUrl = object.mediaSignalingUrl ?? "";
    message.serviceTokens = (object.serviceTokens !== undefined && object.serviceTokens !== null)
      ? ServiceTokens.fromPartial(object.serviceTokens)
      : undefined;
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? Capabilities.fromPartial(object.capabilities)
      : undefined;
    message.recording = (object.recording !== undefined && object.recording !== null)
      ? RecordingConfig.fromPartial(object.recording)
      : undefined;
    message.mediaServerId = object.mediaServerId ?? "";
    return message;
  },
};

function createBaseServiceTokens(): ServiceTokens {
  return { agent: undefined, recorder: undefined };
}

export const ServiceTokens: MessageFns<ServiceTokens> = {
  encode(message: ServiceTokens, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agent !== undefined) {
      ParticipantToken.encode(message.agent, writer.uint32(10).fork()).join();
    }
    if (message.recorder !== undefined) {
      ParticipantToken.encode(message.recorder, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceTokens {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceTokens();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agent = ParticipantToken.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.recorder = ParticipantToken.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceTokens {
    return {
      agent: isSet(object.agent) ? ParticipantToken.fromJSON(object.agent) : undefined,
      recorder: isSet(object.recorder) ? ParticipantToken.fromJSON(object.recorder) : undefined,
    };
  },

  toJSON(message: ServiceTokens): unknown {
    const obj: any = {};
    if (message.agent !== undefined) {
      obj.agent = ParticipantToken.toJSON(message.agent);
    }
    if (message.recorder !== undefined) {
      obj.recorder = ParticipantToken.toJSON(message.recorder);
    }
    return obj;
  },

  create(base?: DeepPartial<ServiceTokens>): ServiceTokens {
    return ServiceTokens.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServiceTokens>): ServiceTokens {
    const message = createBaseServiceTokens();
    message.agent = (object.agent !== undefined && object.agent !== null)
      ? ParticipantToken.fromPartial(object.agent)
      : undefined;
    message.recorder = (object.recorder !== undefined && object.recorder !== null)
      ? ParticipantToken.fromPartial(object.recorder)
      : undefined;
    return message;
  },
};

function createBaseParticipantToken(): ParticipantToken {
  return { participantId: "", token: "" };
}

export const ParticipantToken: MessageFns<ParticipantToken> = {
  encode(message: ParticipantToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.participantId !== "") {
      writer.uint32(10).string(message.participantId);
    }
    if (message.token !== "") {
      writer.uint32(18).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParticipantToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantToken();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.participantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantToken {
    return {
      participantId: isSet(object.participantId)
        ? globalThis.String(object.participantId)
        : isSet(object.participant_id)
        ? globalThis.String(object.participant_id)
        : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
    };
  },

  toJSON(message: ParticipantToken): unknown {
    const obj: any = {};
    if (message.participantId !== "") {
      obj.participantId = message.participantId;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<ParticipantToken>): ParticipantToken {
    return ParticipantToken.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParticipantToken>): ParticipantToken {
    const message = createBaseParticipantToken();
    message.participantId = object.participantId ?? "";
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseCapabilities(): Capabilities {
  return { attachAgent: false, startRecording: false };
}

export const Capabilities: MessageFns<Capabilities> = {
  encode(message: Capabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attachAgent !== false) {
      writer.uint32(8).bool(message.attachAgent);
    }
    if (message.startRecording !== false) {
      writer.uint32(16).bool(message.startRecording);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.attachAgent = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startRecording = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capabilities {
    return {
      attachAgent: isSet(object.attachAgent)
        ? globalThis.Boolean(object.attachAgent)
        : isSet(object.attach_agent)
        ? globalThis.Boolean(object.attach_agent)
        : false,
      startRecording: isSet(object.startRecording)
        ? globalThis.Boolean(object.startRecording)
        : isSet(object.start_recording)
        ? globalThis.Boolean(object.start_recording)
        : false,
    };
  },

  toJSON(message: Capabilities): unknown {
    const obj: any = {};
    if (message.attachAgent !== false) {
      obj.attachAgent = message.attachAgent;
    }
    if (message.startRecording !== false) {
      obj.startRecording = message.startRecording;
    }
    return obj;
  },

  create(base?: DeepPartial<Capabilities>): Capabilities {
    return Capabilities.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Capabilities>): Capabilities {
    const message = createBaseCapabilities();
    message.attachAgent = object.attachAgent ?? false;
    message.startRecording = object.startRecording ?? false;
    return message;
  },
};

function createBaseRecordingConfig(): RecordingConfig {
  return { enabled: false };
}

export const RecordingConfig: MessageFns<RecordingConfig> = {
  encode(message: RecordingConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordingConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordingConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordingConfig {
    return { enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false };
  },

  toJSON(message: RecordingConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<RecordingConfig>): RecordingConfig {
    return RecordingConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecordingConfig>): RecordingConfig {
    const message = createBaseRecordingConfig();
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseDispatchConversationResponse(): DispatchConversationResponse {
  return { conversationId: "", sessionId: "", roomId: "", status: "" };
}

export const DispatchConversationResponse: MessageFns<DispatchConversationResponse> = {
  encode(message: DispatchConversationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conversationId !== "") {
      writer.uint32(10).string(message.conversationId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(26).string(message.roomId);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchConversationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDispatchConversationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DispatchConversationResponse {
    return {
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: DispatchConversationResponse): unknown {
    const obj: any = {};
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<DispatchConversationResponse>): DispatchConversationResponse {
    return DispatchConversationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DispatchConversationResponse>): DispatchConversationResponse {
    const message = createBaseDispatchConversationResponse();
    message.conversationId = object.conversationId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

export type GatewayDispatchServiceService = typeof GatewayDispatchServiceService;
export const GatewayDispatchServiceService = {
  dispatchConversation: {
    path: "/port.gateway.v1.GatewayDispatchService/DispatchConversation" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DispatchConversationRequest): Buffer =>
      Buffer.from(DispatchConversationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DispatchConversationRequest => DispatchConversationRequest.decode(value),
    responseSerialize: (value: DispatchConversationResponse): Buffer =>
      Buffer.from(DispatchConversationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DispatchConversationResponse => DispatchConversationResponse.decode(value),
  },
} as const;

export interface GatewayDispatchServiceServer extends UntypedServiceImplementation {
  dispatchConversation: handleUnaryCall<DispatchConversationRequest, DispatchConversationResponse>;
}

export interface GatewayDispatchServiceClient extends Client {
  dispatchConversation(
    request: DispatchConversationRequest,
    callback: (error: ServiceError | null, response: DispatchConversationResponse) => void,
  ): ClientUnaryCall;
  dispatchConversation(
    request: DispatchConversationRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DispatchConversationResponse) => void,
  ): ClientUnaryCall;
  dispatchConversation(
    request: DispatchConversationRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DispatchConversationResponse) => void,
  ): ClientUnaryCall;
}

export const GatewayDispatchServiceClient = makeGenericClientConstructor(
  GatewayDispatchServiceService,
  "port.gateway.v1.GatewayDispatchService",
) as unknown as {
  new (
    address: string,
    credentials: ChannelCredentials,
    options?: Partial<ClientOptions>,
  ): GatewayDispatchServiceClient;
  service: typeof GatewayDispatchServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
