// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: port/api/v1/voice_runtime.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "port.api.v1";

export interface ResolveLeaseRequest {
  leaseId: string;
  conversationId: string;
  sessionId: string;
}

export interface ResolveLeaseResponse {
  stt?: SttRuntime | undefined;
  llm?: LlmRuntime | undefined;
  tts?: TtsRuntime | undefined;
}

export interface SttRuntime {
  apiKey: string;
  model: string;
  language: string;
}

export interface LlmRuntime {
  apiKey: string;
  model: string;
}

export interface TtsRuntime {
  apiKey: string;
  model: string;
  language: string;
  voiceId: string;
}

function createBaseResolveLeaseRequest(): ResolveLeaseRequest {
  return { leaseId: "", conversationId: "", sessionId: "" };
}

export const ResolveLeaseRequest: MessageFns<ResolveLeaseRequest> = {
  encode(message: ResolveLeaseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.conversationId !== "") {
      writer.uint32(18).string(message.conversationId);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveLeaseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveLeaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveLeaseRequest {
    return {
      leaseId: isSet(object.leaseId)
        ? globalThis.String(object.leaseId)
        : isSet(object.lease_id)
        ? globalThis.String(object.lease_id)
        : "",
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: ResolveLeaseRequest): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<ResolveLeaseRequest>): ResolveLeaseRequest {
    return ResolveLeaseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResolveLeaseRequest>): ResolveLeaseRequest {
    const message = createBaseResolveLeaseRequest();
    message.leaseId = object.leaseId ?? "";
    message.conversationId = object.conversationId ?? "";
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseResolveLeaseResponse(): ResolveLeaseResponse {
  return { stt: undefined, llm: undefined, tts: undefined };
}

export const ResolveLeaseResponse: MessageFns<ResolveLeaseResponse> = {
  encode(message: ResolveLeaseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stt !== undefined) {
      SttRuntime.encode(message.stt, writer.uint32(10).fork()).join();
    }
    if (message.llm !== undefined) {
      LlmRuntime.encode(message.llm, writer.uint32(18).fork()).join();
    }
    if (message.tts !== undefined) {
      TtsRuntime.encode(message.tts, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveLeaseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveLeaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stt = SttRuntime.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.llm = LlmRuntime.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tts = TtsRuntime.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveLeaseResponse {
    return {
      stt: isSet(object.stt) ? SttRuntime.fromJSON(object.stt) : undefined,
      llm: isSet(object.llm) ? LlmRuntime.fromJSON(object.llm) : undefined,
      tts: isSet(object.tts) ? TtsRuntime.fromJSON(object.tts) : undefined,
    };
  },

  toJSON(message: ResolveLeaseResponse): unknown {
    const obj: any = {};
    if (message.stt !== undefined) {
      obj.stt = SttRuntime.toJSON(message.stt);
    }
    if (message.llm !== undefined) {
      obj.llm = LlmRuntime.toJSON(message.llm);
    }
    if (message.tts !== undefined) {
      obj.tts = TtsRuntime.toJSON(message.tts);
    }
    return obj;
  },

  create(base?: DeepPartial<ResolveLeaseResponse>): ResolveLeaseResponse {
    return ResolveLeaseResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResolveLeaseResponse>): ResolveLeaseResponse {
    const message = createBaseResolveLeaseResponse();
    message.stt = (object.stt !== undefined && object.stt !== null) ? SttRuntime.fromPartial(object.stt) : undefined;
    message.llm = (object.llm !== undefined && object.llm !== null) ? LlmRuntime.fromPartial(object.llm) : undefined;
    message.tts = (object.tts !== undefined && object.tts !== null) ? TtsRuntime.fromPartial(object.tts) : undefined;
    return message;
  },
};

function createBaseSttRuntime(): SttRuntime {
  return { apiKey: "", model: "", language: "" };
}

export const SttRuntime: MessageFns<SttRuntime> = {
  encode(message: SttRuntime, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== "") {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.model !== "") {
      writer.uint32(18).string(message.model);
    }
    if (message.language !== "") {
      writer.uint32(26).string(message.language);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SttRuntime {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSttRuntime();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.language = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SttRuntime {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : "",
      model: isSet(object.model) ? globalThis.String(object.model) : "",
      language: isSet(object.language) ? globalThis.String(object.language) : "",
    };
  },

  toJSON(message: SttRuntime): unknown {
    const obj: any = {};
    if (message.apiKey !== "") {
      obj.apiKey = message.apiKey;
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    return obj;
  },

  create(base?: DeepPartial<SttRuntime>): SttRuntime {
    return SttRuntime.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SttRuntime>): SttRuntime {
    const message = createBaseSttRuntime();
    message.apiKey = object.apiKey ?? "";
    message.model = object.model ?? "";
    message.language = object.language ?? "";
    return message;
  },
};

function createBaseLlmRuntime(): LlmRuntime {
  return { apiKey: "", model: "" };
}

export const LlmRuntime: MessageFns<LlmRuntime> = {
  encode(message: LlmRuntime, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== "") {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.model !== "") {
      writer.uint32(18).string(message.model);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LlmRuntime {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLlmRuntime();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LlmRuntime {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : "",
      model: isSet(object.model) ? globalThis.String(object.model) : "",
    };
  },

  toJSON(message: LlmRuntime): unknown {
    const obj: any = {};
    if (message.apiKey !== "") {
      obj.apiKey = message.apiKey;
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    return obj;
  },

  create(base?: DeepPartial<LlmRuntime>): LlmRuntime {
    return LlmRuntime.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LlmRuntime>): LlmRuntime {
    const message = createBaseLlmRuntime();
    message.apiKey = object.apiKey ?? "";
    message.model = object.model ?? "";
    return message;
  },
};

function createBaseTtsRuntime(): TtsRuntime {
  return { apiKey: "", model: "", language: "", voiceId: "" };
}

export const TtsRuntime: MessageFns<TtsRuntime> = {
  encode(message: TtsRuntime, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== "") {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.model !== "") {
      writer.uint32(18).string(message.model);
    }
    if (message.language !== "") {
      writer.uint32(26).string(message.language);
    }
    if (message.voiceId !== "") {
      writer.uint32(34).string(message.voiceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TtsRuntime {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTtsRuntime();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.voiceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TtsRuntime {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : "",
      model: isSet(object.model) ? globalThis.String(object.model) : "",
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      voiceId: isSet(object.voiceId)
        ? globalThis.String(object.voiceId)
        : isSet(object.voice_id)
        ? globalThis.String(object.voice_id)
        : "",
    };
  },

  toJSON(message: TtsRuntime): unknown {
    const obj: any = {};
    if (message.apiKey !== "") {
      obj.apiKey = message.apiKey;
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.voiceId !== "") {
      obj.voiceId = message.voiceId;
    }
    return obj;
  },

  create(base?: DeepPartial<TtsRuntime>): TtsRuntime {
    return TtsRuntime.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TtsRuntime>): TtsRuntime {
    const message = createBaseTtsRuntime();
    message.apiKey = object.apiKey ?? "";
    message.model = object.model ?? "";
    message.language = object.language ?? "";
    message.voiceId = object.voiceId ?? "";
    return message;
  },
};

export type VoiceRuntimeServiceService = typeof VoiceRuntimeServiceService;
export const VoiceRuntimeServiceService = {
  resolveLease: {
    path: "/port.api.v1.VoiceRuntimeService/ResolveLease" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveLeaseRequest): Buffer => Buffer.from(ResolveLeaseRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveLeaseRequest => ResolveLeaseRequest.decode(value),
    responseSerialize: (value: ResolveLeaseResponse): Buffer =>
      Buffer.from(ResolveLeaseResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveLeaseResponse => ResolveLeaseResponse.decode(value),
  },
} as const;

export interface VoiceRuntimeServiceServer extends UntypedServiceImplementation {
  resolveLease: handleUnaryCall<ResolveLeaseRequest, ResolveLeaseResponse>;
}

export interface VoiceRuntimeServiceClient extends Client {
  resolveLease(
    request: ResolveLeaseRequest,
    callback: (error: ServiceError | null, response: ResolveLeaseResponse) => void,
  ): ClientUnaryCall;
  resolveLease(
    request: ResolveLeaseRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveLeaseResponse) => void,
  ): ClientUnaryCall;
  resolveLease(
    request: ResolveLeaseRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveLeaseResponse) => void,
  ): ClientUnaryCall;
}

export const VoiceRuntimeServiceClient = makeGenericClientConstructor(
  VoiceRuntimeServiceService,
  "port.api.v1.VoiceRuntimeService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): VoiceRuntimeServiceClient;
  service: typeof VoiceRuntimeServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
